<?xml version="1.0" encoding="utf-8"?><!-- 默认按钮样式 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 禁用状态 -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/dp_999" />
            <solid android:color="@color/color_circle_button_selector_disabled" />
        </shape>
    </item>

    <!-- 按压状态 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/dp_999" />
            <solid android:color="@color/color_bottom_navigation_on_surface" />
        </shape>
    </item>

    <!-- 焦点状态 -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/dp_999" />
            <solid android:color="@color/color_bottom_navigation_on_surface" />
        </shape>
    </item>

    <!-- 默认状态 -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/dp_999" />
            <solid android:color="@color/color_bottom_navigation_surface" />
        </shape>
    </item>

</selector>